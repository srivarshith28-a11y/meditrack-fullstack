package com.meditrak;

/**
 * Placeholder class retained only because the file could not be deleted in this environment.
 * The project now runs as a normal Spring Boot application and does not use external Tomcat deployment.
 */
public final class ServletInitializer {
    private ServletInitializer() {
    }
}
